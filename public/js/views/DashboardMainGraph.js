'use strict';

define([
    'backbone',
    'backbone/View'
  ], function() {

  return Backbone.View.extend({
    initialize: function(options) {
      this.render();
    },

    events: {
    },

    onClose: function() {
    },

    render: function() {
    }
  });
});
